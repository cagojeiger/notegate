global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: notegate
    metrics_path: /metrics
    dns_sd_configs:
      - names:
          - web
        type: A
        port: 9191
        refresh_interval: 15s
