//! File command input and output DTOs.

pub use notegate_model::files::{
    ChildrenCursor, ChildrenPage, ChildrenRequest, CreateFile, CreateFolder, CreateText,
    DeleteNode, DeleteResult, Edit, FileContent, FileStats, FileView, MoveNode, NodeView,
    PatchResult, PatchText, ReadContent, ReadResult, ReadText, ReadTextBody, StoredContent,
    StoredFile, TextStats, TextView, WriteTarget, WriteText, WriteTextBody,
};
