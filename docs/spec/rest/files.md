# REST Files

File은 binary/object content node다. 직접 text read/patch/grep 대상이 아니다.

현재 REST file upload/download endpoint는 노출하지 않는다. `file_objects` schema는 향후 file surface를 위해 준비되어 있다.
