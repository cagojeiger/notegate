# 2026-07-14

## Follow-up

- [ ] Object 정리 실패에 종료 상태(dead-letter)를 추가한다.
  - 현재 `mark_cleanup_failed`는 `retry_count`만 증가시키고 backoff는 1시간에서 clamp될 뿐 포기하지 않는다.
  - 물리 삭제가 영구 실패하면 `delete_pending`/`expire_pending`에 영원히 머물며 매 cycle 재claim되고, `expired`/`deleted`가 되지 않아 `purge_terminal_history`가 회수하지 못한다 → 고착 row 무한 증가.
  - retry_count 상한 초과 시 `delete_failed` 등 종료 상태로 격리하고(정리 partial index에서 제외) 운영 알림을 낸다.
  - 마이그레이션(state enum + CHECK + index)과 worker 재시도 로직 변경이 필요하므로 독립 PR로 진행한다.
  - 관련: `db/src/object_storage_repo.rs` `mark_cleanup_failed`, `api/src/object_storage_cleanup_worker.rs` `cleanup_retry_seconds`.
